# 2026-09-03

## Complete-picture run (agent, from the "form a complete picture" objective)
- Output: `tasks/20260911-112058/picture.md` — one consolidated description of every stage from host rim to
  emitted C under the 2026-05-28 framing. Structure: framing recap; 16 stages in stack order, each
  Claim / Status (ruled | direction | open) / Open; consolidated blank spots; opens sharpened for
  ruling; targeted spec edits. Full-corpus read, precedence 2026-08-30 + `docs/new_spec/` >
  `dialogue/` > July + `docs/spec/` > `project/` > `reducer/` > project_history.
- No new semantics were decided. Every blank spot is named where it sits and again in one list.

## What the read confirmed (agent, [fact] unless marked)
- Stages **ruled** and mutually consistent: lexer (ADR-1), grammar (C6+R1, block promotion, group
  erasure, `opcode ::= $ labeled_expression`, nyad), lowering (13 rules, `{@}` always), triage core
  (rules 0-3 verbatim, branch-first), binders/identifiers (binder axiom, Model B, lexical `$fn`
  scoping, identifier matrix), bootstrap `let` (A' final shape), observation *in principle*.
- Stages **direction only**: host rim (WASI-shaped), `exec`, vmeta, vsystem.
- Stages **open**: CESK proper, the pith table, `$form`, metadata list, ZINC edges.
- July's L5 catalogue splits cleanly: effect labels, `ARG`/`R3`/`RESHAPE`, callee-first `RUN-APPLY`,
  TERM threading and `SHAPE-APPLY`-for-pending-control survive; `SHAPE-APPLY` over `{@}` data and
  `RESTORE-DELTA1/2` (TERM-wrapping escaping structural results) are dead under the binder axiom.

## New findings this run (agent)
- [fact] "Evaluation order is pinned by the spec" is a ruling with no sentence behind it — the spec
  lists rules 0-3 but never writes the order down. Named as a blank spot; TODO added to `03_v0.md`.
- [fact] The inert-`$` note recorded on 2026-08-30 as "added to 03_v0.md opcodes section" was not in
  the file. Added this run.
- [assumption] ZINC is currently the only July *implementation* decision carried as if current. It is
  an optimization hypothesis about closures with known remaining arity, and today's picture has one
  construct (`$fn`) plus opcode-states-that-are-closures. Proposed demotion to "re-derive after CESK".
- [fact] `$rec`'s "dead" ruling was scoped to the `let` form; "separate `$form`/`$rec` allowed where
  needed" was said of opcodes. Unresolved as stated; named rather than reconciled.
- [fact] Where `let` lives is not ruled — vmeta form vs inert-`$` form vs core opcode. `03_v0.md`
  still carried the dead `$let: x is: ...` sketch as a core opcode.
- [fact] `reducer/` is further behind than the audit implies: the tokenizer still treats `^` as the
  delta token and `~` as a prefix operator (inverted from the nyad ruling), has no ADR-1 adjacency
  classification, and `bytecode_source.c` still emits `:group` where lowering rule 4 now erases parens.
  `OP_FN0/1/2` and the `e0b7b3f` Rule-3 arity regression (`reducer_reducer.c:235`) stand as known.
- [fact] Under today's ruling the Rule-3 regression is *sharper*, not weaker: `{@}` data must not be
  executed by Rule 3 at all, while a pending machine application must be, to expose its root shape.

## Opens put to the user (agent proposals, none ruled)
- O1 pith of `{@}` — lean list-shaped, duality falls out of list piths; cost is that application
  becomes indistinguishable from a 3-list under observation.
- O2 pith of `[{:id}, {name}]` — lean two-element list, with dereference a property of the reference
  in demand position, not of the pith. The two readings differ exactly where duality is tested.
- O3 metadata elision — lean one generic rule: piths skip the metadata slot, so a rebuilt node is
  pith-identical, not byte-identical. This defines what duality means precisely.
- O4 `exec` contract — (a) accepts any value or only `{@}`-rooted; (b) "apply-once" = one rule / one
  saturation / run-to-value; (c) trap halts the host (lean: yes, matching the uncatchable-trap ruling).
- O5 where `let` lives — lean inert-`$` form consumed at build time, keeping `$fn` the only executable
  construction form.
- O6 ZINC standing — confirm demotion to hypothesis.
- O7 the `reducer/` Rule-3 regression — lean fix now, independently of the machine rewrite, since the
  test suite is the only executable check the project has.
- O8 `$rec` — restate scope of "dead", or leave open explicitly.

## Targeted spec edits applied (agent, mechanical supersessions only)
- `docs/new_spec/02_syntax.md`: `{@}` restated as an ordinary inert node type, observable through its
  pith, with no source spelling (supersedes "isn't part of the syntax / the single exception");
  pith `TODO: add more` replaced by the enumerated missing-pith list; metadata TODO extended with the
  pith-elision consequence.
- `docs/new_spec/03_v0.md`: dangling reduction-rules `TODO:` pointed at the CESK section plus the
  branch-first/order note; inert-`$` note added to opcodes; `$let` sketch marked SUPERSEDED by the
  ruled `let[...] do ... end` with the "where does let live" question named, not answered; `$form`
  added as a named open core form.
- `docs/spec/02_concrete_syntax.md`: obsolescence banner, matching `docs/spec/04_vf.md` and
  `tasks/20260711-131255/v0_layers.md`.
- Deliberately not done: no CESK rules, no pith definitions. `docs/spec/01_introduction.md` (superseded
  wholesale) and `docs/spec/03_v0.md` (still the usable CESK seed) left unbannered, noted in the picture.

## Process note
- `memsearch` is not installed in this environment, so `memsearch index .memsearch/memory/` was not run.
  Reindex locally before relying on search over this entry.

## Illustrated appliances added to the picture (agent, on user request)
- `tasks/20260911-112058/picture.md` Part II: 14 imagined use cases — vsystem (generic vector module, tagged
  unions + exhaustive match, defer/error unions/try, SOA layout as data, build-time execution,
  whole-program test/reflection tables, build-as-program with convention rules, `vrust`-style
  ownership pass, C interop at two levels) and vmeta (pass as `$fn` over terms with `match`/`case`,
  C AST as terms + printer, defining and mandating a pass, the driver, staging without quasiquote).
  Inspirations named per case: zig, Odin, Jai, Rust, C++ (rejections named too: C++ templates,
  Odin `context`, Rust runtime trait dispatch). [assumption] throughout; grammar untouched — every
  keyword-looking word is an identifier head, every form one of the five application shapes.
- Corpus evidence from hand-checking every snippet against `02_syntax.md` (§II.15):
  juxtaposition tax (`pub fn`, `try x`, `else do` → `@[pub] fn`, `try(x)`, `else: (do ... end)`
  or adjacent-statement pairing); annotation on its own line is orphaned by ASI (`]` ends an
  expression) — house style `@[x] fn` or exempt the annotation's `]`; multi-line label chains need
  `...`; match arms are `case pat do ... end`, not `pat -> e`; `!T` free via prefix op, postfix `?`
  impossible, `^` not an operator char; typed literals via `f{bytes}` vindicated.
- New blank spots (Part III #13, #14): lowering of *mixed* infix chains is unwritten (rule 12 shows
  one `{op}`); ASI inside a `do...end` nested in `(...)` — "bare" is ambiguous.
- Ordering finding: vmeta cannot be sketched past a ten-line pass without O1 (pith of `{@}`, needed
  by spine-flattening matchers) and `$form` (needed by `<~` recursive passes). Those two order the
  next run.
