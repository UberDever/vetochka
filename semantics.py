# This module will contains semantical analysis for scopes and transformations
# to lower the language into into scopes and uses (strip all syntax sugar)
# Then, bytecode module will transform this down to tree-calculus
