# 11.07.2026

## S3 agent-knowledge migration

- Decision: Move chronological project history, LLM opcode research, CESK draft, and
  calculus-basis review into `.memsearch/memory/`; keep normative docs, current
  architecture snapshots, examples, and project rationale at their existing paths.
- Decision: Keep only project-specific memory skill under `.agents/skills/`; generic
  personal skills moved to Pi's global `~/.pi/agent/skills/` location.
- Rejected: Copy generic global skills into repository; they are not Vetochka-specific.
- Open: None.

## Portable global agent state

- Decision: Use `~/.agents/AGENTS.md` as global agent charter and
  `~/.agents/skills/` as sole home for personal cross-harness skills.
- Decision: Keep project memory rules in repository `AGENTS.md`; they depend on
  Vetochka's `.memsearch/memory/` collection and are not global behavior.
- Rejected: Separate personal skill copies under Pi, Claude, Codex, or
  CodeAssistant because they drift and bind shared behavior to individual harnesses.
- Open: Harness-managed system skills may remain in vendor directories because they
  are runtime components, not personal global state.

## User communication preference (16:10)

- Decision: User asked agent to always use caveman full mode for responses.
- Rejected: Normal prose by default; user prefers compressed caveman-full style.
- Open: None.

## v0_layers consistency cleanup (16:20)

- Decision: Fixed `project/v0_layers.md` links to the CESK draft in `.memsearch/memory/`.
- Decision: Demoted opcode stem-shaped completion and Y-combinator/non-strict recursion validation to speculative Layer 5 direction, not settled Layer 1/3 semantics.
- Decision: Reworded inert storage: cells only store nodes; executable roots are activated by Layer 5 after specialization.
- Rejected: Treating `{fn}`/`{form}` examples as current syntax; they remain historical notation only.
- Open: Layer 5 still must settle `LOCAL`, functions, strict/non-strict forcing, argument spine/GRAB, continuations, evaluation order, and exact opcode/function encoding.

## Project artifact migration (17:00)

- Decision: Moved historical `project/` examples/sketches into `.memsearch/memory/artifacts/` as dated markdown artifacts with original paths and exact original content preserved.
- Decision: Updated `project_history.md` links to point at the moved artifacts while keeping chronological log structure.
- Decision: ~~Kept `project/v0_layers.md` in place for now as old snapshot / transition artifact while recomposing current layers in `docs/spec/02_v0_layers.md`.~~ Superseded by shared syntax/v0/vf layout at 19:30; current v0 spec is `docs/spec/03_v0.md`.
- Rejected: Condensing examples by summary only; exact content matters for later archaeology.
- Open: After the new `docs/spec` layers are complete, decide whether `project/v0_layers.md` should also move into memory or stay as a compatibility snapshot.

## v-family syntax split (17:20)

- Decision: ~~Use `v*` as notation for the family of languages above v0; prose name is **v-family**. Do not use `vstar` as current spec name.~~ Superseded by **vf** naming at 17:35 below.
- Decision: ~~Keep v0 in `docs/spec/02_v0_layers.md`; move v-family language material to separate `docs/spec/03_v_family.md` because v-family is a different superset language, not part of v0.~~ Superseded by `docs/spec/02_concrete_syntax.md`, `docs/spec/03_v0.md`, and `docs/spec/04_vf.md` at 19:30 below.
- Decision: Move outdated `docs/grammar.md` to `.memsearch/memory/artifacts/grammar.md`; it mixed v0 syntax, extended syntax, cell encoding, and runtime opcode protocol.
- Decision: Annotation payloads are comma-lists because they parse/process easier and read better.
- Open: ~~Exact split between future `vmeta` and `vsystem`; exact v-family lowering rules to v0; list preservation vs desugaring per v-family language.~~ Superseded by vf open note at 17:35 below.

## vf naming and list policy (17:35)

- Decision: Supersede "v-family" prose name with **vf** for `v*` languages. `vstar` is rejected.
- Decision: `vmeta` and `vsystem` remain far-future speculative use-case names only; do not actively design them now.
- Decision: vf languages are not lowered by compiler developers as fixed product features. Programmers write language code using v0 protocols for introspection, runtime term forging, and compilation.
- Decision: ~~Ordinary list syntax should not preserve `:list`; it desugars to proper lists. Special source-level list needs should use an explicit tagged special form.~~ Superseded by 19:45: existing vf syntax must be used unless a required semantic distinction is demonstrated.
- Open: Exact v0 protocols exposed for programmer-written vf compilation/introspection/term forging.

## L3 specialization draft (17:50)

- Decision: ~~L3 specializes normalized L2 syntax plus context into closed executable cells with abstract vocabulary `DELTA`, `I64`, `BYTES`, `APPLY`, `LOCAL(n)`, and `OPCODE(state)`.~~ Superseded by activation-time specialization at 18:10 below.
- Decision: ~~L3 performs only context-dependent compilation: lexical name resolution, fixed native registry resolution, recognized executable construction conventions, cell emission, and diagnostics. Pure syntactic rewrites stay in L2.~~ Superseded by activation-time specialization at 18:10 below.
- Decision: Names die in L3; runtime has no name lookup. Unbound non-native names are specialization errors.
- Decision: No current v0 `GLOBAL` semantic node; external values enter by explicit parameters/application, not hidden environment injection.
- Decision: `LOCAL` and `OPCODE` are real executable node families introduced by L3; exact L1 wire tags remain open.
- Open: ~~Exact `do!` protocols, native registry inventory, `LOCAL`/`OPCODE` wire tags, and grouping behavior for quoted syntax.~~ Superseded by activation-time/open syntax-data wording at 18:10 below.

## Quoting rule (18:00)

- Decision: Vetochka as a whole has no language-level quoting rule. This is central to using triage calculus: code/data treatment comes from term structure and introspection, not a separate quote special form.
- Rejected: Adding a language-level quote construct as a core semantic mechanism.
- Open: Specs should avoid describing quoted syntax as if Vetochka has a built-in quote rule; if syntax preservation is needed, describe explicit term/data protocols instead.

## Activation-time specialization (18:10)

- Decision: Specialization happens when a term enters executable position and is activated by an executable-position protocol, not through a user-visible `{specialize}` function/opcode.
- Decision: Any value may be placed in executable position; malformed or non-executable normalized syntax fails at activation.
- Decision: The specialization context is supplied by the executable position (e.g. `do! fn: [x] do ... end` body context), not by ambient dynamic name lookup.
- Decision: Forged/loaded/parsed/runtime-constructed terms remain porous data until activated; activation can specialize them later.
- Decision: Vetochka has no language-level quote rule; specs must describe explicit syntax-data/activation protocols instead of quoting.
- Open: Exact executable-position protocols, especially `do!`, remain to be specified.

## Spec syntax discipline (18:25)

- Decision: Do not use invented pseudo-language syntax in specs. Use existing Vetochka syntax only; if syntax is missing, treat that as a design problem to solve first.
- Decision: Dialogue may use pseudo-code sparingly, but prefer established languages such as Python when explaining concepts because their semantics are already familiar and less likely to confuse design claims.
- Rejected: Protocol-level pseudo-code in normative spec text.
- Open: When examples need syntax not yet designed, mark the syntax gap explicitly instead of inventing examples.

## treecalcul.us triage verification (18:25)

- Evidence: Checked `https://treecalcul.us/specification/` (with browser User-Agent). It defines grammar `E ::= △ | E E`, triage rules 1/2/3a/3b/3c, calls irreducible expressions "values or programs", says values are binary trees of leaves/stems/forks, and says rules 3a-c make calculus intensional by reflecting on values.
- Decision: This supports Vetochka's no-language-level-quote rule: programs are values/terms structurally; reflection is by triage shape, not a separate quote construct.
- Clarification: The Vetochka statement "role comes from position" is an added language/runtime discipline over this substrate: a stored term has no intrinsic data/executable tag; data position passes it as value, executable position activates/specializes it.
- Open: Exact executable-position protocols remain to be specified.

## L3 role-from-position invariant (18:35)

- Decision: Added L3 invariant to spec: terms have no intrinsic executable/data tag; role comes from position.
- Decision: Data position treats a term as ordinary value. Executable position activates it as syntax under an executable-position protocol.
- Decision: A term denoting identifier syntax resolves through binders only when activated in an executable context that supplies those binders; outside executable position it remains data.
- Evidence: treecalcul.us supports no-quote substrate by treating irreducible expressions as values/programs and reflection as triage over value shape.
- Open: Exact executable-position protocols remain future work.

## Triangle notation (18:45)

- Decision: Prefer `△` (white up-pointing triangle, U+25B3) for Unicode triage-calculus notation because it matches treecalcul.us; keep `^` as terminal-friendly ASCII source spelling.
- Rejected: Greek `Δ` as current spec notation; it is a different character and should not be used except in migration notes.
- Open: Current lexer recognizes Greek `Δ`; replace or supplement with `△` later.

## L4 backbone facilities (18:45)

- Decision: Keep `load_file` and `parse_term` as v0 backbone facilities for programmer-defined module/code graph systems.
- Decision: `load_file :: path-bytes -> bytes | diagnostic` is the only required host effect in v0; module roots, imports, dependency resolution, caching, and path policy stay above v0.
- Decision: `parse_term :: source-bytes -> normalized syntax term | diagnostic` is pure and returns ordinary term data with no executable meaning.
- Decision: v0 has no `specialize`, `execute`, or `eval` intrinsic; execution is term entering executable position, which invokes L3 activation-time specialization and then L5 execution.
- Rationale: Lowest useful substrate for reusable code is host bytes plus parser-to-term. Programmers can then define module systems and vf languages using v0 protocols.
- Open: Diagnostic term shape; `load_file` path authority/interpretation; whether `parse_term` accepts only v0 syntax or full shared vf parser substrate without assigning vf semantics; concrete parsed-term representation.

## `$` executable construction and `{@}` application (19:10)

- Decision: `$` replaces provisional `do!` spelling. Lexer/parser treats it as special token admitted wherever identifier callee can occur; subsequent labels/application use ordinary grammar.
- Decision: `$ f: x do: x + 1` structurally follows ordinary callee application. In `$` application context `do:` is a label; bare `do ... end` remains block syntax. Parser needs this disambiguation.
- Decision: `$` is always bound only when activated in executable position, where it selects executable-construction protocol; it is not runtime opcode.
- Decision: Replace old normalized-term application marker `{$}` with `{@}`. `{@}` is normalized-term data mapping to `APPLY`; it does not conflict with L2 annotation parser form `@[...]`.
- Open: Exact `$` protocol states/labels and function construction semantics.

## Shared concrete syntax spec (19:30)

- Decision: Shared lexer/parser/normalization syntax belongs in separate user-facing `docs/spec/02_concrete_syntax.md`, before v0 (`03_v0.md`) and vf (`04_vf.md`) semantics.
- Decision: Reserved block words followed immediately by `:` are labels in every labeled-argument position: `do:` and `end:`; bare `do ... end` remains block syntax.
- Decision: Concrete syntax owns `{@}` normalized application marker and documents its separation from source annotation punctuation `@[`.
- Decision: `$` is special lexer token but participates in ordinary postfix/labeled application; `$ f: x do: x + 1` has ordinary application structure.
- Open: Exact `$` executable-construction protocol semantics and parse diagnostic term shape.

## vf syntax substrate rule (19:45)

- Decision: Remove open question about special source-level list forms; current syntax already provides sufficient structure for programmer-defined data and DSLs.
- Decision: Ordinary lists desugar to proper lists with no `:list` preservation.
- Decision: Application, labels, lists, annotations, prefix/infix shape, selectors, and byte payloads are vf syntax substrate. Parser supplies structure only; programmer-written vf code assigns meaning.
- Decision: Do not add literal grammar merely for prettier data structures. Require a demonstrated semantic distinction existing syntax cannot preserve.

## `parse_term` syntax scope (19:55)

- Decision: `parse_term` accepts full shared Vetochka syntax, not v0-only syntax. It returns normalized inert term data and assigns neither vf nor executable semantics.
- Open: Concrete representation/ownership of parsed normalized terms.

## vf inclusion rule (20:10)

- Decision: v0 is a vf subset. Any v0 code may occur unchanged as ordinary vf code; this is semantic inclusion, not merely shared parser acceptance.
- Decision: vf adds source forms and programmer-written interpretation above v0, without a special bridge into v0.
- Decision: `$` occurs unchanged in vf as inherited v0 code; its protocol semantics remain defined by v0.

## Opcode privacy and function protocol (20:35)

- Decision: No in-place term rewrites unless explicitly introduced later as a justified optimization.
- Decision: `$f: x do: body` is eager one-argument function with single-expression `do:` body. `$fn: [x, y, ...] do body-forms end` is eager multi-argument function. `$form` is not v0.
- Decision: `LOCAL(n)` is L3 output only; raw syntax and ordinary programs have no direct constructor.
- Decision: Every opcode uses generic `OPCODE(state)` cell representation. State identifies opcode kind, transition, and arity; no per-opcode cell family/layout.
- Decision: Source `$` specializes on first activation into generic opcode state; subsequent labels apply to transient opcode state.
- Decision: Opcodes expose stable public term/cell structure, including Rule 3 shape. Their private runtime state is unforgeable.
- Decision: Lexical environment/captures remain private implementation details; no general capture/environment inspection or forging facility.
- Open: Exact public opcode structure/shape projection/state fields; closure payload; `$fn` sequencing account; `with:` semantics; whether eager `$f` thunks replace K for delayed lexical code.

## Opcode saturation projection (20:45)

- Decision: Describe opcode state as **unsaturated** or **saturated**, not leaf/stem/fork.
- Decision: When Rule 3 demands structural observation, an unsaturated opcode projects as a stem. Applying an argument that saturates opcode state dispatches immediately; no saturated opcode value is observable as fork.
- Decision: Leaf/stem/fork remain delta vocabulary; use them only for triage projection at opcode boundary.
- Open: Exact stable public `OPCODE(state)` structure and state fields.

## `$fn` block input normalization (21:00)

- Decision: Bare `do ... end` supplied to `$fn` remains positional block term normalized as `[{:block}, S1, S2, ...]`; do not rewrite it to `do: block` label.
- Decision: Generic opcode public input history preserves both labeled inputs and positional term inputs in application order. State determines each input meaning.

## Label normalization in opcode inputs (21:10)

- Decision: Labeled source input `name: value` normalizes directly to `[{:name}, value]`; no generic `:label` wrapper.
- Decision: Generic opcode input history is ordered ordinary terms. It contains tagged label terms, positional block terms such as `[{:block}, S1, S2, ...]`, and ordinary postfix call/list/string terms.
- Supersedes 21:00 wording that input history has separate labeled/positional variants; labels are ordinary tagged terms after normalization.

## Label normalization correction (21:20)

- Correction: Current implementation (`reducer/bytecode_source.c`) and historical grammar normalize `name: value` as `[{:label}, {name}, value]`.
- Decision: Keep generic `:label` normalization in current spec. Dynamic `{:name}` label tags are not decided.
- Supersedes 21:10 claim that labels normalize directly to `{:name}`.

## Opcode state and input policy (21:30)

- Decision: Public opcode state is a stable flat global integer enum. Debug names are non-semantic.
- Decision: Each opcode state declares next-input policy. v0 currently has `syntax` (retain normalized term) and `eager` (evaluate in caller context before transition); future policies must be explicit.
- Decision: Eager input produces `V`, then fresh state appends `V`; syntax input appends original normalized term. No in-place rewriting or observable half-transition.
- Decision: `$` protocols are runtime opcode forms, not parser special forms.
- Open: first-class thunk/force semantics.

## Opcode colloquium pause / resume order (21:45)

Settled:
- Public `OPCODE` state is flat global integer enum; debug names non-semantic.
- Accepted public projection: `^ i64(state) inputs`; `inputs` is ordered proper list and private payload unreachable. `eval_step` recognizes opcode nodes explicitly.
- State input policies are explicit: `syntax`, `eager`, and `route`.
- `route` accepts raw `[{:label}, {selector}, payload]`, selects arm, then applies arm payload policy. `$load_file` / `$parse_term` selected `$` source route.
- Eager argument evaluates in caller context, then result is appended in fresh state; no observable half-transition.
- Do not preserve distinct list/string source spelling in opcode input storage.

Deferred order:
1. exact argument records + L2-to-opcode boundary (candidate `[:pos,^,payload] | [:label,key,payload]`);
2. saturation/dispatch/error/continued-application behavior;
3. `$SELECT` state table and IDs;
4. closure/E/LOCAL account;
5. eager evaluation order/extent and application arity;
6. thunk/force;
7. `$fn` sequencing;
8. `with:` protocol.

Implementation assessment verified by `zig build test-all` (pass): cells, triage reducer, source parser/formatter, and source-to-cell encoder exist. Current encoder still recognizes historical `{fn}` and emits `OP_FN0/1/2`; there is no generic `OPCODE`, L3 lexical specialization/LOCAL, runtime opcode dispatch, closures, effects, or L4 backbone implementation. Therefore v0 0.1 is not near feature completion despite useful substrate.

## Final opcode argument records (resume)

- Decision: Opcode input records are `[:pos, ^, payload] | [:label, key, payload]`, held in ordered proper-list input history.
- Decision: L2 labels already normalize as the `:label` record. On opcode entry, any non-label postfix operand, including a block `[{:block}, ...]`, is wrapped as `:pos`. Do not retain list/string source spelling metadata.

## Opcode transition outcomes (resume)

- Decision: Every opcode transition returns exactly `Continue(next-state, inputs)`, `Dispatch(result)`, or `Error(diagnostic)`.
- Decision: Dispatch has no observable saturated opcode; remaining postfix application applies normally to the dispatch result.
- Model: `$` constructs effectful state machines; dispatch is where their designated action occurs or begins.

## Authoritative v0 opcode table (resume)

- Decision: Full v0 opcode spec uses an authoritative table: ID, opcode state, input shape, policy, outcome (including uniform diagnostic). Mermaid is projection only.
- Decision: Allocate flat numeric state IDs once after all v0 state names/transitions are approved.

## Hard traps versus user errors (resume)

- Decision: Execution-time protocol failures are uncatchable hard traps in v0. Host execution state records optional diagnostic term handle and halts; an invalid handle means no error. Existing `reducer_t._error` textual field should become this handle.
- Decision: Do not elaborate diagnostic term contents yet.
- Decision: User-defined/recoverable errors are ordinary `[{:ok}, value] | [{:error}, diagnostic]` data. vf operates on these values, not v0 traps.

## Opcode specification presentation (resume)

- Decision: Authoritative full v0 opcode specification is a nested tree per state with ID, input shape, policy, outcome, and traps. Mermaid is only a readable projection. This supersedes the prior table presentation decision.

## Immediate function-parameter validation (resume)

- Decision: `$SELECT` immediately validates `f` and `fn` parameter payloads before their continuation states. Malformed binder/parameter-list syntax hard-traps while routing.

## `$f` body specialization (resume)

- Decision: `$f` dispatch immediately specializes `do:` body under current lexical environment extended with validated binder. Result function carries closed executable code plus opaque capture; do not re-specialize raw body on every call.

## v0 explicit eager boundary (resume)

- Decision: For executable `APPLY(A,B)`, determine callee A first and retain B as code until callee state policy requires otherwise.
- Decision: v0 eagerly evaluates only call operands of function values made by `$f`/`$fn`, and routed `$load_file`/`$parse_term` payloads. `$f`/`$fn` construction binders, parameter lists, and bodies are syntax.
- Decision: Raw `foo: bar` is inert term data; it has no v0 name lookup or implicit elaboration and hard-traps if used as an unrecognized executable callee.

## `$f` body input (resume)

- Decision: `F_AFTER_PARAM` accepts only syntax `[:label, {do}, body]`. It retains label/body as code and dispatches closure construction. Positional bare block or other label hard-traps.

## `$fn` body input (resume)

- Decision: `FN_AFTER_PARAMS` accepts its body only as positional syntax `[:pos, ^, [{:block}, S1, S2, ...]]`; reject `do: expression`.

## `$fn with:` effect order (resume)

- Decision: v0 is effectful and curried. Transition/source order defines host-effect and hard-trap order. `$fn with:` before versus after its body may observably differ by design.

## Function-local elimination (eagerness pass)

- Decision: Function call eliminates/instantiates all reachable `LOCAL` bindings needed by returned value before frame death. Internal inspection may see live locals; public return must not contain dead `LOCAL`.

## Closed eager-function body before L0 (eagerness pass)

- Decision: Before any L0 reduction in an eager function body, recursively instantiate resolved locals from call frame plus opaque closure capture into fresh closed body term.
- Consequence: `APPLY(^, LOCAL(0))` with V becomes `APPLY(^, V)`, then L0 rule 0a returns `^ V`; no dead local can enter result.

## Recursive activation and inert lists (eagerness pass)

- Decision: Activation recursively resolves identifier syntax throughout activated terms, including lists/nested structures, but does not evaluate subterms unless runtime rule says so.
- Decision: Only lexical binders resolve; other identifiers in executable activation trap. Arbitrary identifier syntax is forgeable as `[{:id}, {name-bytes}]` data and becomes executable only under matching lexical binder.
- Decision: List evaluation returns instantiated proper L0 list structure; it does not independently evaluate elements.

Closure status:
- Settled: `$f` body is specialized at construction; closure retains opaque outer capture; call eagerly evaluates operand, then fresh body instantiates locals/captures before L0; no dead locals escape.
- Open: exact callable opcode states/arity, E representation/lifetime, `$fn` multi-argument/block semantics, tail/partial/overapplication, thunk/force.

## `TERM` runtime family supersedes close-copy (eagerness pass)

- Decision: ~~Before L0 reduction, eagerly instantiate/close-copy entire function body.~~ Superseded: use real `TERM(root; private E)` runtime family, like `OPCODE`.
- Decision: Function call runs body under retained TERM environment; direct LOCAL lookup occurs only on executable demand. Triage selection propagates E to each selected branch, so structural values may escape without traversal/closing copy.
- Decision: TERM E is opaque/unforgeable and must be transparent to public structural operations; wrapped list and its root list are not distinguishable. Mutation is outside v0.
- Open: exact TERM triage-view/application/nesting/lifetime semantics.

## TERM semantic/implementation split (eagerness pass)

- Decision: Open terms semantically carry objective `TERM(root,E)`. CEK/CESK may thread E transiently without wrapper allocation, but preserves `(root,E)` TERM whenever it returns, is structurally stored, becomes pending argument, or crosses continuation/frame.

## Evaluation-rule record (eagerness pass)

- Working evaluation rules now maintained concisely in `dialogue/v0-layers-restoration.md` under `Working v0 evaluation rules`.
- Decision: `TERM(APPLY(f,x),E)` evaluates callee under E with pending `TERM(x,E)`.
- Decision: delta stores pending TERM argument unchanged, preserving its caller E; delta never forces it.

## Lexical-reference comparison / pause record (eagerness pass)

- Wrote full comparison at `dialogue/lexical-reference-models.md`: Model A resolved `LOCAL` executable IR vs Model B nameful code paired with persistent TERM environment.
- Stable: TERM/captured lexical E, inert lists, TERM triage propagation, and no ambient dynamic scope are independent of the lexical-reference choice.
- Open: choose LOCAL Model A or nameful Model B. LOCAL is no longer treated as demonstrated semantic necessity; it is an executable-IR choice. Model B trades static/definition-time unknown-name checking for demand-time lookup traps and preserves source names in executable code.
- Updated `docs/spec/03_v0.md` to specify settled TERM behavior and explicitly mark lexical-reference representation open; removed its premature assertion that names die/LOCAL is settled.

## Selected nameful TERM lexical model (eagerness pass)

- Decision: Select Model B: nameful executable code paired with persistent captured TERM environment. `TERM([{:id},{name}],E)` resolves name through E only on executable demand.
- Decision: `LOCAL` is rejected as v0 semantic IR/cell ABI. It may later be a private resolved-slot cache only if it exactly preserves Model B semantics.
- Consequences: no body-wide resolver/S scope; function closure stores raw body, binder names, Edef; call extends Edef with eager values. Unknown identifiers hard-trap on demand.
- Updated comparison, ledger evaluation rules, and `docs/spec/03_v0.md`.

## `to:` application syntax (resume)

- Decision: `to:` is ordinary label syntax, with state-specific v0 runtime meaning. `$f`/`$fn` closures consume it as call operand after constructor dispatch; `$cons` will use it as second construction operand.
- Decision: Parenthesized postfix call has no v0 function-call semantics; shared parser retains it for vf.
- Open: `to:` argument ordering, repeated use, and overapplication.

## ID resolution at triage demand (resume)

- Decision: ID resolves through captured TERM E at the single node-demand point, including triage view; triage observes resolved node.
- Consequence/open: unfinished `$f` cannot expose raw binder ID through its triage view without resolving it. Candidate custom opcode projection exposes binder name bytes/syntax form instead; exact opcode public traversal/provenance remains open.
- Updated ledger and spec wording.

## Application-first term semantics (resume)

- Decision: Terms are application-first. `^ ^ ^` means `((^ ^) ^)`; leaf/stem/fork storage shape arises through Rules 0a/0b, not source constructors. `$f`/`$fn` likewise remain application chains until active demand.
- Updated dialogue evaluation rules and L0 spec note.

## Rule 3 is structural, not forcing (resume)

- Decision: Rule 3 obtains current triage view only; it does not force APPly reduction. APPly lens may expose binary application chain structurally. REF/ID transparent traversal is distinct from APPly evaluation.

## Pure triage view recognizes delta application spine (correction)

- Correction: `(^ u v) == ((^ u) v)` syntactically. Pure triage view recognizes `APPLY(^,u)` as stem and `APPLY(APPLY(^,u),v)` as fork without requiring prior special-node reduction. Arbitrary application, including `$f` chain headed by `$`, is not thereby fork.

## Evaluation/lens reconciliation (resume)

- Reconciled active ledger: Model B nameful TERM is accepted; `ID` resolves only when active lens treats it as executable usage, while construction/declaration lenses preserve syntax.
- Correct fact: labels are encoded term structure (current source encoder uses tagged `:label` DELTA tree); no CESK label side channel exists. Lenses are the added CESK traversal state.
- Structural restart: execution of `TERM(APPLY(f,x),E)` is callee-first with pending x; Rule 3 is structural and does not execute pending application. Pure triage recognizes only delta-headed application spines as Leaf/Stem/Fork.
- Immediate open task: define strict-cell `$`/OPCODE lens over raw APPLY spine with encoded labels, including non-executing Rule 3 projection and child provenance.

## Partial L5 spec record (resume)

- Added `L5 Execution machine` to `docs/spec/03_v0.md` and contents. It records the hybrid: L0 term rewriting plus L5 small-step CESK-style transitions; callee-first pending application; TERM/Model B; non-forcing Rule 3; delta application-spine triage; REF; generic OPCODE; label term data; and `$`/`to:` decisions.
- Moved runtime semantics out of L3. Deliberately open: configuration grammar/rule catalogue, lenses, normal-form stopping, exact opcode traversal/projection children, and unfinished function protocols.

## Scoped pure triage congruence (resume)

- Decision: Specify `≡₀` only for effect-free pure L0 cell representation: `APPLY(DELTA0,x) ≡₀ DELTA1(x)` and `APPLY(DELTA1(x),y) ≡₀ DELTA2(x,y)`. It is not v0 equality and not an L5 rewrite permission in effectful contexts; L5 preserves order through explicit transitions.
- Added this to L1/L5 spec and active ledger.

## Rule 3 shape-demand resumption (resume)

- Decision: Rule 3 rewrite itself does not reduce application, but when L5 shape demand sees top-level `APPLY` as its argument it schedules that application and resumes Rule 3 through retained continuation. It must never classify APPLY by generic storage arity.
- This replaces the old reducer's Rule-3 error on APPLY with intended future semantics; current generic-arity behavior is a June refactor regression.
- Updated L5 spec and ledger wording.

## Supersession: pure triage congruence

- ~~Specify `≡₀` as a semantic primitive for pure L0 cell representation.~~ Superseded: L5 is specified solely by explicit operational transitions, including Rules 0a/0b. Any observational equivalence of representations is a later derived theorem, not a rewrite/equality rule.
- Removed `≡₀` from normative spec; ledger supersession noted.

## Pure triage small-step catalogue (resume)

- Added normative L5 pure-delta machine rules using `⟨RUN|SHAPE|RETURN, continuation⟩`, with `ARG`, `R3`, and `RESHAPE` continuations. Includes Rules 0a/0b/1/2/3 and top-level APPLY shape-demand/resumption, without semantic equivalence.

## Environment-threaded triage/TERM rules (resume)

- Rewrote normative triage machine as `⟨request,E,continuation⟩`; added TERM entry/restoration, ID lookup, TERM application, shape return/restoration, and E-capturing delta shape transitions.
- Decision: active E is machine state. TERM entry installs it; restoring a structural delta result materializes one root TERM; selected shape children receive TERM association. This replaces eager per-child wrapper construction.
