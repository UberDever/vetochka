# 2026-09-24

## Status of the 2026-09-03 run (user ruling)
- The 2026-09-03 run (`tasks/20260911-112058/picture.md`, its findings, opens O1–O8, and the uncommitted edits
  to `docs/new_spec/02_syntax.md` / `03_v0.md`) was an **experiment**: an agent allowed to vibecode
  and imagine. Treat its content as hypothesis, not ruling or fact.
- ~~"Targeted spec edits applied" (2026-09-03)~~ disregarded by user, see this entry. The diff is kept
  for reference at `tasks/20260911-112058/experiment-2026-09-03-spec-edits.md`.

## Move to tatr (user rulings)
- (user) Task tracking uses tatr. Two artifact kinds: `docs/` = user-facing docs (80-90% correct,
  developed further); `tasks/` = development-process artifacts. `dialogue/`, `project/` and
  `.memsearch/memory/` are abandoned; their files moved whole into task folders.
- (user) Task ID = time the note was first written: first commit introducing the line (git pickaxe), UTC.
  Untracked notes use file mtime. ~~Same-second collisions get `-01`, `-02` suffixes.~~ superseded (user, 2026-09-24): suffixes are for
  per-person IDs; a collision takes the next free second, as the tatr README says.
- (user) Epics first, tagged `epic`; sub-items that are not tasks on their own go in the epic's `items.md`.
  Finished (✅) and rejected (❌) items are included as CLOSED tasks (`rejected` tag for ❌).
- (user) TODOs in docs and code are replaced by task references.
- (agent) [fact] Three ⬜ items were closed on later recorded rulings: s-prefix (grammar has bare `{}`),
  do...end sequencing and PTC (2026-08-30 CESK pre-rulings). Reasons are in each TASK.md.
- (agent) [fact] The 2026-09-03 experiment's spec edits were reverted; the diff lives in its task.
- Open: old `.memsearch/memory` chunks remain in the shared memsearch collection (reset would also drop
  another project's index).

## Tasks as source of truth (user rulings)
- (user) Tasks are a database of all design labour, as important as code. They state current knowledge
  as plain lists with provenance, not migrated quotes. Rewritten accordingly the same day.
- (user) tatr runs through the pi-box (`box tatr`). memsearch uses a per-project collection; no shared memory.
- (user) Repo-local memory skill removed; the tatr skill carries its rules.
- (user) Licensing decided the same day: see task 20260924-083540.
