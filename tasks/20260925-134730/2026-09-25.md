# 2026-09-25

## `{@}`, cells and Rule 3
- Decision (user): runtime objects are first-class and storable; machine state (pending applications, TERM) is not a runtime object.
- Decision (user): Rule 3 never evaluates; applications persist and are observed like any node; identifier dereference is the only indirection; bind with `$fn` to inspect a result. Supersedes the 2026-08-30 exception for pending applications. Departs from treecalcul.us on purpose.
- Decision (user): an application, like any runtime node, may have its own pith: storage and observation are separate choices.
- Direction (user): tags for semantic objects, not storage layouts; piths as stateful views held in variables.
- Rejected (agent claims, corrected by user): "list or cell decides what Rule 3 sees"; "deep force" of trees holding applications (those applications are data).
- Open: the pith of an application; its storage.
