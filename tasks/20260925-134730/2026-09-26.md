# 2026-09-26

## Opcodes, exec, archive
- Decision (user): opcodes and special instructions are lists headed by a short special string the evaluator recognizes; just a string to the language; close to their pith forms. Supersedes 2026-07-11 per-state node types.
- Decision (user): `exec` is one step: one applicable rule is chosen and executed; CESK rules define evaluation completely.
- Decision (user): `archive/` is a third artifact kind, a garbage-collection target; never indexed, grepped last. 20 agent notes moved into 7 groups (AGENTS.md, Archive).
