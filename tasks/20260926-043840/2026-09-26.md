# 2026-09-26

## Intent tree, literals, spec review
- Decision (user): five intents (syntax; programs as data; languages over a shared core; the C frontend; bookkeeping); epics become initiatives, except the hand-written spec epic, dissolved into the `spec` tag. See the ledger and mapping in this folder.
- Rejected (user): dedupe-only and staged restructures (option B chosen); merging host interaction with host rim and bootstrap (split kept).
- Decision (user): literals come as tagged pure trees and as tagged trees with an opaque payload (task 20260926-053139).
- Decision (user): the spec's rewrite rules show applications as `[{@}, meta, f, x]` data; rule 2 shows the whole `$` form.
- Lesson (agent): removing a section to the next heading also removed rulings appended below it; a loss check over the diff caught it before commit.
- Open: literal tags and meta (20260926-053139); placement of the intro-sources task (20260805-162237).
